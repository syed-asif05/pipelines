name: Hello World Demo

on:
  push:
    branches:
      - main

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
        - name: Checkout code
          uses: actions/chweckout@v3

        - name: Print Hello
          run: echo "Hello, GitHub Actions!"
           
      

      
        
        
      
